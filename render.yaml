databases:
  - name: laravel-db
    plan: free
    region: singapore

services:
  - type: web
    name: my-laravel-app
    runtime: docker
    plan: free
    region: singapore
    envVars:
      - key: APP_KEY
        sync: false
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_URL
        sync: false
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase: { name: laravel-db, property: host }
      - key: DB_PORT
        fromDatabase: { name: laravel-db, property: port }
      - key: DB_DATABASE
        fromDatabase: { name: laravel-db, property: database }
      - key: DB_USERNAME
        fromDatabase: { name: laravel-db, property: user }
      - key: DB_PASSWORD
        fromDatabase: { name: laravel-db, property: password }
      - key: AUTORUN_ENABLED
        value: "true"
      - key: AUTORUN_LARAVEL_MIGRATION
        value: "true"
      - key: AUTORUN_LARAVEL_CONFIG_CACHE
        value: "true"
